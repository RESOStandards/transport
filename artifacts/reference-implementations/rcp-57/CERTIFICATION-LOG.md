# Certification Log

Generated by `src/write-log.ts`. **Do not edit.** Regenerate with
`npm run log` after any change to the specification, the scenarios or the suite.

The checks are read from Section 3 of
[`offer-management.md`](https://github.com/RESOStandards/transport/blob/main/proposals/offer-management.md).
Coverage is read from the test suite's own results – 141 tests, run
2026-09-25 18:29Z, all passing.
Neither is transcribed, so this log cannot quietly disagree with either.


**21 of 38 verified.** 17 not yet, 0 drifted.

A check is *verified* only when a scenario bound to it ran in the suite and passed.
A scenario declared in the registry with no test behind it shows as **claimed, not
run** – declaring one does not raise the number. A check is *drifted* when the
specification reworded it after its scenario was written, which means the scenario
may no longer test what it claims; drift keeps its coverage so the ambiguity stays
visible instead of disappearing.

## By Section

| Section | Verified | Total |
| :--- | ---: | ---: |
| Section 2.4 | 4 | 7 |
| Section 2.11 | 5 | 5 |
| Section 2.2 | 0 | 5 |
| Section 2.9 | 5 | 5 |
| Section 2.5 | 3 | 4 |
| Section 2.7 | 3 | 4 |
| Section 2.1 | 0 | 2 |
| Section 2.6 | 1 | 2 |
| Section 2.8 | 0 | 2 |
| Section 2.12.7 | 0 | 1 |
| Section 2.3 | 0 | 1 |

## Every Check

| Id | Section | Status | Scenarios | Kind | Check |
| :--- | :--- | :--- | :--- | :--- | :--- |
| S3-01 | Section 2.4 | not verified |  |  | The candidate MUST serve the resources, fields, types and nullability of Section 2.4 through Section 2.6, and a payload it produces MUST validate as RESO Common Format against the declared Data Dictionary version. |
| S3-02 | Section 2.7 | not verified |  |  | The candidate MUST accept and serve the standard values of Section 2.7 and MUST reject a multi-valued status on either side. |
| S3-03 | Section 2.5 | not verified |  |  | The candidate MUST use the existing standard values for `BuyerFinancing` and `Concessions`, and MUST NOT substitute offer-specific equivalents (Section 2.5). |
| S3-04 | Section 2.9 | verified | `offer-needs-published-listing` | rejects | The candidate MUST NOT accept an offer that references no published listing activity (Section 2.9). |
| S3-05 | Section 2.5 | verified | `submission-correlates-to-offer` | resolves | Every `OfferSubmission` the candidate accepts MUST correlate to an `Offer` it holds, by `OfferId`. A candidate that accepts a submission correlating to no offer fails (Section 2.5). |
| S3-06 | Section 2.6 | verified | `property-group-resolves`, `property-group-inline-or-by-key` | resolves, accepts | Every `OfferSubmission` the candidate accepts MUST carry an `OfferPropertyGroupKey` that resolves, whether the group travels inline or by key (Section 2.6). |
| S3-07 | Section 2.5 | verified | `either-payload-form` | accepts | The candidate MUST accept a submission both on its own and expanded into its `Offer` under `Submissions`, and MUST NOT require the expanded form (Section 2.5). |
| S3-08 | Section 2.4 | verified | `second-offer-both-live`, `second-offer-after-ended` | accepts | The candidate MUST accept a second `Offer` from the same buyer on the same listing, both where an earlier offer has ended and where both are live. A candidate that rejects it as a duplicate, or that merges it into the earlier offer, fails (Section 2.4). |
| S3-09 | Section 2.4 | verified | `ended-offer-takes-no-turns` | rejects | The candidate MUST NOT append a submission to an offer that has been withdrawn, rejected or has expired (Section 2.4). |
| S3-10 | Section 2.4 | verified | `coordinate-needs-system-member`, `coordinate-refused-before-store` | present, rejects | An `Offer` the candidate accepts MUST carry `ListingId` or `ListingKey`, and MUST carry at least one of `OfferOriginatingSystemName` or `OfferOriginatingSystemId`. An `Offer` carrying `OfferUoi` and neither of that pair fails. A candidate that accepts a listing identifier with no organization or system member fails (Section 2.4). |
| S3-11 | Section 2.6 | not verified |  |  | An `OfferPropertyGroup` the candidate accepts MUST identify the property by one of the two permitted combinations (Section 2.6). |
| S3-12 | Section 2.4 | not verified |  |  | Where the candidate publishes a hashed coordinate, it MUST be reproducible: the same listing MUST yield the same value on repeated construction (Section 2.4). |
| S3-13 | Section 2.9 | verified | `superseded-submission-frozen`, `counter-leaves-prior-intact` | immutable | On a counter, the candidate MUST create a new `OfferSubmission`, and every submission that was already superseded MUST remain byte-identical to what it held when the counter was created (Section 2.9). A candidate that modifies a superseded submission fails. |
| S3-14 | Section 2.5 | verified | `expansion-is-read-only` | refuses | The candidate MUST NOT create, alter or remove a submission through the `Submissions` expansion (Section 2.5). |
| S3-15 | Section 2.7 | verified | `no-terms-act-adds-no-submission` | absent | The candidate MUST record an act that changes no terms by setting its own status field on the current submission, and MUST NOT create a submission for it. A candidate that emits a submission for an acknowledgement, acceptance, rejection or withdrawal fails (Section 2.7). |
| S3-16 | Section 2.7 | verified | `current-state-is-highest-sequence` | derives | The current state the candidate reports for an offer MUST equal the pair of statuses on its highest-sequence submission, at every point in the exchange (Section 2.7). |
| S3-17 | Section 2.7 | verified | `only-own-side-status`, `disagreeing-statuses-allowed` | immutable, accepts | The candidate MUST NOT set the counterparty's status field, and MUST NOT reject an offer whose two status fields disagree (Section 2.7). |
| S3-18 | Section 2.9 | verified | `sequence-is-max-plus-one`, `sequence-is-not-a-count` | sequence | Every submission the candidate creates MUST carry `OfferSubmissionSequence` set to one greater than the highest it has seen for that `OfferId`, or to 1 for the first, and MUST keep that value unchanged thereafter (Section 2.9). |
| S3-19 | Section 2.9 | verified | `tie-breaks-on-organization` | sequence | Where two submissions of one offer carry the same sequence, the candidate MUST order them by the submitting party's Unique Organization Identifier ascending, and MUST NOT order them by arrival time (Section 2.9). |
| S3-20 | Section 2.9 | verified | `prior-turns-retrievable`, `turns-in-sequence-order` | sequence | Every submission created during the test MUST remain retrievable under its `OfferId` after later submissions are created, and MUST be returned in `OfferSubmissionSequence` order (Section 2.9). |
| S3-21 | Section 2.2 | not verified |  |  | Every activity the candidate posts MUST use only Activity Streams 2.0 vocabulary and MUST NOT carry custom JSON-LD terms (Section 2.2). |
| S3-22 | Section 2.2 | not verified |  |  | An activity the candidate posts MUST NOT carry offer content. That content MUST be reachable only through the protected link the activity references (Section 2.2). |
| S3-23 | Section 2.8 | not verified |  |  | The candidate MUST use the mapped Activity Streams type for every state that has one, and MUST use a `Note` rather than an invented type for every state that does not (Section 2.8). |
| S3-24 | Section 2.4 | not verified |  |  | The candidate MUST NOT address an activity carrying offer content to the public collection, and an activity it addresses to the public collection MUST carry no element of Section 2.4 through Section 2.7 (Section 2.1). |
| S3-25 | Section 2.1 | not verified |  |  | The candidate MUST NOT fail an inbound activity on the ground that it was addressed to the public collection, where that activity carries no offer content (Section 2.1). |
| S3-26 | Section 2.8 | not verified |  |  | The candidate MUST NOT fail when a counterparty publishes no state in the thread, and MUST resolve the state from the payload instead (Section 2.8). |
| S3-27 | Section 2.3 | not verified |  |  | The candidate MUST NOT parse an activity identifier to recover offer data, and MUST NOT require an identifier to contain an `OfferId` (Section 2.3). |
| S3-28 | Section 2.12.7 | not verified |  |  | The candidate MUST NOT post an `Undo` for an activity another party posted (Section 2.12.7). |
| S3-29 | Section 2.1 | not verified |  |  | The actor the candidate posts under MUST resolve to a Unique Organization Identifier or a Unique System Identifier: its own, that of the organization it acts for, or that of the system making the requests (Section 2.1). |
| S3-30 | Section 2.4 | verified | `name-only-coordinate-accepted` | accepts | The candidate MUST accept a listing coordinate whose organization member is an originating system name or identifier, and MUST NOT reject an offer on the ground that no organization identifier was supplied for the listing (Section 2.4). |
| S3-31 | Section 2.11 | verified | `payload-refuses-unauthenticated` | refuses | Every payload link the candidate publishes MUST refuse an unauthenticated dereference (Section 2.11). |
| S3-32 | Section 2.2 | not verified |  |  | Where a payload dereference fails, the candidate MUST leave the offer's state unchanged. A candidate that records a withdrawal, rejection, expiry or acceptance in response to a timeout, a `5xx` or a `404` fails (Section 2.2). |
| S3-33 | Section 2.2 | not verified |  |  | The candidate MUST reject a malformed or truncated payload whole, and MUST still hold the last payload it read successfully (Section 2.2). |
| S3-34 | Section 2.2 | not verified |  |  | Every payload the candidate has served MUST remain reachable for as long as its offer is live (Section 2.2). |
| S3-35 | Section 2.11 | verified | `identity-from-token-only` | derives | The candidate MUST determine a requester's identifier from the presented token and MUST NOT infer it from a value carried in the request (Section 2.11). |
| S3-36 | Section 2.11 | verified | `no-delegated-entitlement` | refuses | The candidate MUST NOT accept another participant's assertion that a requester is entitled to an offer in place of its own determination (Section 2.11). |
| S3-37 | Section 2.11 | verified | `non-party-refused` | refuses | The candidate MUST refuse to serve offer content to a requester outside the parties to that offer (Section 2.11). |
| S3-38 | Section 2.11 | verified | `withheld-buyer-fields-not-an-error` | present | The candidate MUST NOT treat the absence of buyer or co-buyer fields as an error (Section 2.11). |

## What "Not Verified" Means Here

It means no scenario observes the check, not that a candidate would fail it. The
distinction matters because a certification tool that tests a subset without
saying so reads as a passing grade – which is the failure this log exists to
prevent.
