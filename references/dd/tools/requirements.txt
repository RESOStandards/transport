# DD reference tooling — single runtime dependency.
# openpyxl is the only sanctioned XLSX library: it preserves cell formatting, comments, and merged
# cells on write (SheetJS strips them). Pinned for reproducible CI; bump deliberately.
openpyxl==3.1.5
